name: Render video
on:
  workflow_dispatch:
    inputs:
      brandImg:
        default: "https://assets.kogan.com/files/brand-logos/ergolux-brand-logo.png"
        required: true
        description: "test"
      heroImg:
        default: "https://assets.kogan.com/files/product/2021/FS700CALTHA/FS700CALTHA_3.jpg?auto=webp&canvas=1080%2C1920&fit=bounds&height=1920&quality=75&width=1080"
        required: true
        description: "test"
      firstImg:
        default: "https://gorgeous-selkie-5dd26b.netlify.app/assets/exclusive-prices.png"
        required: true
        description: "test"
      productTitle:
        default: "Adidas Womens Big Logo Fleece Hoodie"
        required: true
        description: "test"
      salePriceDollars:
        default: "399"
        required: true
        description: "test"
      salePriceCents:
        default: ""
        required: false
        description: "test"
      papDollars:
        default: "499"
        required: true
        description: "test"
      papCents:
        default: ""
        required: false
        description: "test"
      heroDimensions:
        default: "196.921875, -43.828125, 1"
        required: true
        description: "test"
      simpleDate:
        default: "2022-08-31"
        required: true
        description: "test"
jobs:
  render:
    name: Render video
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@main
      - uses: actions/setup-node@main
      - uses: FedericoCarboni/setup-ffmpeg@v1
      - run: npm i
      - run: echo $WORKFLOW_INPUT > input-props.json
        env:
          WORKFLOW_INPUT: ${{ toJson(github.event.inputs) }}
      - run: npm run build -- --props=./input-props.json
      - uses: actions/upload-artifact@v2
        with:
          name: video.mp4
          path: out/video.mp4
